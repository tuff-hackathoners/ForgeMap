/**
 * AI Client
 * Calls into the AI service (Person 1) at AI_SERVICE_URL.
 * Passes photo buffers and project state, receives structured JSON.
 */

// TODO: implement AI service integration

export {};
